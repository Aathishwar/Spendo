{
  "name": "spendo-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Spendo sync server: Postgres, one /api/sync endpoint, and the PWA on the same origin.",
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "migrate": "node src/migrate.js",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "pg-mem": "^3.0.5"
  }
}
